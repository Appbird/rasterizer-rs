use crate::world::actor::Actor;

struct World {
    actors:Vec<Actor>,
    map_id:Vec<usize>
}
impl World {
    fn spawn(: Actor) {
        
    }
    fn destroy_obj(, removed_obj:Vec<usize>) {

    }
}